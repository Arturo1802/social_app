/** @type {import('next').NextConfig} */
const nextConfig = {
  images:{
    domains:[
      'lh3.googleusercontent.com',
      'cdn.sanity.io'
    ]
  },
  reactStrictMode: true,
}

module.exports = nextConfig
